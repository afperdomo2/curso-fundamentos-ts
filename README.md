### Ejecutar TypeScript (Tiene en cuenta el archivo: `tsconfig.ts`):

```
npx tsc
```

### Compilar y ejecutar los archivos TypeScript en un proyecto de forma continua:

```
npx tsc --watch
```

### Crear el archivo de configuración `tsconfig.ts`:

```
npx tsc --init
```
